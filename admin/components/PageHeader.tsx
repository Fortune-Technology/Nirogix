// The page-title block is now the shared `@hms/ui` component, so the Admin console
// and the Portal cannot drift apart. Re-exported here so existing imports keep working.
export { PageHeader, type PageHeaderProps } from '@hms/ui';
