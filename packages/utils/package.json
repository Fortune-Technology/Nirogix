{
  "name": "@hms/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared framework-agnostic utilities (formatting, dates, currency, ids, validation helpers).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@hms/config": "*",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
