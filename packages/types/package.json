{
  "name": "@hms/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types and API contracts consumed by hms_backend and hms_frontend.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hms/config": "*",
    "typescript": "^5"
  }
}
