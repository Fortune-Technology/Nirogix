// @hms/types — shared TypeScript types and API contracts.
// Consumed by hms_backend (API) and hms_frontend (Portal) so request/response
// shapes stay in sync across the wire.
//
// Populated in Stage 0 (Platform Foundation).
// See resources/development-plan.md §6, §10 and resources/architecture.md (Technology Stack).
export {};
