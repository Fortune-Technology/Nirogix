{
  "name": "@hms/client",
  "version": "0.0.0",
  "private": true,
  "description": "Shared frontend client foundation \u2014 typed API errors, the HTTP core (token + silent refresh + one feedback surface), the session/permissions context, and the permission guards. Consumed by every Nirogix frontend (ADR-054).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hms/types": "*",
    "@hms/ui": "*"
  },
  "peerDependencies": {
    "react": ">=19"
  },
  "devDependencies": {
    "@hms/config": "*",
    "@types/react": "^19",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
