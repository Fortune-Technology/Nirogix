{
  "name": "@hms/permissions",
  "version": "0.0.0",
  "private": true,
  "description": "Dot-hierarchy permission keys (module.submodule.page.action) shared by hms_backend and hms_frontend — the single source of truth for permission strings.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hms/config": "*",
    "typescript": "^5"
  }
}
