{
  "name": "hms",
  "version": "0.0.0",
  "private": true,
  "description": "Enterprise HMS monorepo — Takoriya Technology LLP",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "prettier": "^3",
    "turbo": "^2.5.0",
    "typescript": "^5"
  }
}
