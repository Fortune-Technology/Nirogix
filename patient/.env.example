# Patient portal (:3002) — copy to .env and change the value. Every key below is live
# (never commented). Hosts per environment: resources/domains.md.

# Backend API base URL — the patient portal talks only to the API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
