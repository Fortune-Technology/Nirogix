// The page-title block is now the shared `@hms/ui` component, so the Portal and the
// Admin console cannot drift apart. Re-exported here so existing imports keep working.
export { PageHeader, type PageHeaderProps } from "@hms/ui";
