# marketing site — copy to .env.local. Never commit .env.local.
# Where the "Login" action sends users — the Portal's login route.
NEXT_PUBLIC_PORTAL_LOGIN_URL=http://localhost:3000/login
# Dev port is set via the `next dev -p 3001` script; override here only if needed.
